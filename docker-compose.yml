services:
  api:
    build: .
    ports:
      - "${PORT:-5010}:${PORT:-5010}"
    env_file:
      - .env
    volumes:
      - ~/.codex/auth.json:/app/codex_credentials.json
    restart: unless-stopped
